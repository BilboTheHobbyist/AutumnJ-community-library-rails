<% comments.all.each do |comment| %>

  <ul><i>Comment: </i><%= comment.content %><br>
  <i>User: </i><%= comment.user.name %><br>
  <i> Comment submitted: </i><%= comment.updated_at.strftime("%A, %b %e, %Y") %><br></ul>

<% end %>