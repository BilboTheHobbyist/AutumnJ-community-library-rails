<% comments.all.each do |comment| %>

  <ul><i><%= t('.comment') %></i><%= comment.content %><br>
  <i><%= t('.user') %></i><%= comment.user.name %><br>
  <i><%= t('.comment_submitted') %></i><%= comment.updated_at.strftime("%A, %b %e, %Y") %><br></ul>

<% end %>